<div class="container">
  <h1 class="my-4">Badges</h1>

  <div class="row">
    <% @badges.each do |badge| %>
      <div class="col-md-4 mb-4">
        <div class="card">
          <div class="card-body">
            <h5 class="card-title"><%= badge.title %></h5>
            <p class="card-text"><%= badge.description %></p>
            <%= image_tag badge.image, class: "lrg-badge-image" if badge.image.attached? %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
